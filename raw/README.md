# Raw Promoted Runs

Promoted run artifacts are stored under `raw/runs/<run-id>/`.

This is public-safe raw capture output, not the local operator workspace. CAPTCHA images, answers, tokens, and bridge event streams are intentionally excluded.

